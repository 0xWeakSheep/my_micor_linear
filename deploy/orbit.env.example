# Example: https://orbit.your-company.com
APP_URL=
# Generate three different values with: openssl rand -base64 48
# The container intentionally refuses to start while any value is empty.
AUTH_PASSWORD_PEPPER=
AUTH_TOKEN_PEPPER=
ORBIT_WEBHOOK_ENCRYPTION_KEY=
ORBIT_DEMO_MODE=0
ORBIT_ALLOW_PUBLIC_SIGNUP=0
ORBIT_WEBHOOK_ALLOW_INSECURE_LOCALHOST=0
